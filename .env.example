# Game Theory Workbench - Environment Configuration
# Copy this file to .env and adjust values as needed
#
# Docker Compose will automatically load .env from the project root

# =============================================================================
# Application Mode
# =============================================================================
# Set to "production" for production deployments
# In production mode:
#   - CORS_ORIGINS must be explicitly set
#   - Frontend static files must be built and present
ENVIRONMENT=development

# =============================================================================
# CORS Configuration
# =============================================================================
# Comma-separated list of allowed origins for CORS
# Required in production mode; optional in development (defaults to localhost:5173)
# Example: CORS_ORIGINS=https://example.com,https://www.example.com
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# =============================================================================
# File Upload Limits
# =============================================================================
# Maximum upload file size in bytes (default: 5MB)
MAX_UPLOAD_SIZE_BYTES=5242880

# =============================================================================
# Plugin Service URLs
# =============================================================================
# Inside Docker Compose, the app resolves plugins by service name (the defaults).
# You only need to set these when running the app outside Docker.
#
# Docker (default):          Outside Docker:
#   http://gambit:5001         http://localhost:5001
#   http://pycid:5002          http://localhost:5002
#   http://egttools:5003       http://localhost:5003
#   http://vegas:5004          http://localhost:5004
#   http://openspiel:5005      http://localhost:5005
#
# Uncomment to override:
# GAMBIT_URL=http://localhost:5001
# PYCID_URL=http://localhost:5002
# EGTTOOLS_URL=http://localhost:5003
# VEGAS_URL=http://localhost:5004
# OPENSPIEL_URL=http://localhost:5005
